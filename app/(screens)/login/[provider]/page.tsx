import React from 'react';

import {SafeArea} from '@/app/components/Elements';

const LoginRedirectScreen = async () => (
  <SafeArea className="flex flex-col justify-center items-center">
    <h1>Logging in</h1>
  </SafeArea>
);

export default LoginRedirectScreen;
